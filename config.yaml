# OpenClaw 配置文件
skills:
  load:
    extraDirs:
      - /Users/lvze/work/skills/skills
    watch: true
    watchDebounceMs: 1000

channels:
  telegram:
    enabled: true
    mediaMaxMb: 20
    timeoutSeconds: 120
    # 不设置 proxy，使用系统 HTTP_PROXY/HTTPS_PROXY 环境变量
    webhook:
      enabled: false
